# Quickstart

The canonical quickstart lives at the repository root:
**[QUICKSTART_A3_ISAAC.md](../QUICKSTART_A3_ISAAC.md)**

It walks the full loop — prepare the A3 asset, smoke-test the table-tennis scene,
train the unified forehand/backhand policy, export `hope_pingpong.onnx`, evaluate
`success_rate`, and run the exported policy in the reference simulation with the
planner. This file is only a pointer so older links keep working; do not add
content here.
